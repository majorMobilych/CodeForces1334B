package solutions;

public interface SolutionsInterface {
    /**
     * @param people - array of people's wealth
     * @param minToBeRich - minimal wealth to be reach
     * @return number of max reach people
     */
    int maxRichPeople(int[] people, int minToBeRich);
}
